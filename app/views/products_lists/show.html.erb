<!DOCTYPE html>
<html>
<body>
<head>
  <title>家管理</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
</head>
<div class="w3-quarter w3-container"> </div>
<div class="w3-half w3-container">
  <div class="w3-content w3-display-container container" style="max-width:1500px">
    <table class="w3-table w3-striped">
      <% @products.each do |p| %>
      <tr>
        <td><%= p.id %></td>
        <td><a href=<%= p.image %> style="width: 25%">a</td>
        <td><a href=<%= products_path(id:p.id) %> style="width: 25%"><i class="fa fa-id-card-o fa-fw"></i><%= p.name %></td>
        <td><%= p.description %></td>
        <td><%= p.price %></td>
      </tr>
      <% end %>
    </table>
    <a class="button" href="welcome/index" >返回</a>
  </div>
</div>
<div class="w3-quarter w3-container"></div>
</body>
</html>